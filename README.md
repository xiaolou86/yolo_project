# YOLOv8 Project

## Project Description
This project monitors real-time video streams from cameras and alerts the admin when specific objects are


## test env install
pip install -e .
#pip install opencv-python shapely psutil torch pillow matplotlib tqdm pyyaml requests
